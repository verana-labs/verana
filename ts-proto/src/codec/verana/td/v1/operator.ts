// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: verana/td/v1/operator.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { Timestamp } from "../../../google/protobuf/timestamp";
import Long = require("long");

export const protobufPackage = "verana.td.v1";

/** OperatorAllowance defines the allowance state for an operator acting on behalf of a group */
export interface OperatorAllowance {
  /** group is the group policy address */
  group: string;
  /** operator is the operator account address */
  operator: string;
  /** allowance is the maximum allowed spend per period (in uvna) */
  allowance: number;
  /** usage is the current period usage (in uvna) */
  usage: number;
  /** reset_period_seconds is the duration in seconds after which usage resets (0 = never) */
  resetPeriodSeconds: number;
  /** last_reset_at is when usage was last reset */
  lastResetAt:
    | Date
    | undefined;
  /** last_usage_at is when operator last used their allowance */
  lastUsageAt:
    | Date
    | undefined;
  /** active indicates whether the operator is currently active */
  active: boolean;
}

function createBaseOperatorAllowance(): OperatorAllowance {
  return {
    group: "",
    operator: "",
    allowance: 0,
    usage: 0,
    resetPeriodSeconds: 0,
    lastResetAt: undefined,
    lastUsageAt: undefined,
    active: false,
  };
}

export const OperatorAllowance = {
  encode(message: OperatorAllowance, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.group !== "") {
      writer.uint32(10).string(message.group);
    }
    if (message.operator !== "") {
      writer.uint32(18).string(message.operator);
    }
    if (message.allowance !== 0) {
      writer.uint32(24).uint64(message.allowance);
    }
    if (message.usage !== 0) {
      writer.uint32(32).uint64(message.usage);
    }
    if (message.resetPeriodSeconds !== 0) {
      writer.uint32(40).int64(message.resetPeriodSeconds);
    }
    if (message.lastResetAt !== undefined) {
      Timestamp.encode(toTimestamp(message.lastResetAt), writer.uint32(50).fork()).ldelim();
    }
    if (message.lastUsageAt !== undefined) {
      Timestamp.encode(toTimestamp(message.lastUsageAt), writer.uint32(58).fork()).ldelim();
    }
    if (message.active !== false) {
      writer.uint32(64).bool(message.active);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): OperatorAllowance {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOperatorAllowance();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.group = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.operator = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.allowance = longToNumber(reader.uint64() as Long);
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.usage = longToNumber(reader.uint64() as Long);
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.resetPeriodSeconds = longToNumber(reader.int64() as Long);
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.lastResetAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.lastUsageAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 8:
          if (tag !== 64) {
            break;
          }

          message.active = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OperatorAllowance {
    return {
      group: isSet(object.group) ? globalThis.String(object.group) : "",
      operator: isSet(object.operator) ? globalThis.String(object.operator) : "",
      allowance: isSet(object.allowance) ? globalThis.Number(object.allowance) : 0,
      usage: isSet(object.usage) ? globalThis.Number(object.usage) : 0,
      resetPeriodSeconds: isSet(object.resetPeriodSeconds) ? globalThis.Number(object.resetPeriodSeconds) : 0,
      lastResetAt: isSet(object.lastResetAt) ? fromJsonTimestamp(object.lastResetAt) : undefined,
      lastUsageAt: isSet(object.lastUsageAt) ? fromJsonTimestamp(object.lastUsageAt) : undefined,
      active: isSet(object.active) ? globalThis.Boolean(object.active) : false,
    };
  },

  toJSON(message: OperatorAllowance): unknown {
    const obj: any = {};
    if (message.group !== "") {
      obj.group = message.group;
    }
    if (message.operator !== "") {
      obj.operator = message.operator;
    }
    if (message.allowance !== 0) {
      obj.allowance = Math.round(message.allowance);
    }
    if (message.usage !== 0) {
      obj.usage = Math.round(message.usage);
    }
    if (message.resetPeriodSeconds !== 0) {
      obj.resetPeriodSeconds = Math.round(message.resetPeriodSeconds);
    }
    if (message.lastResetAt !== undefined) {
      obj.lastResetAt = message.lastResetAt.toISOString();
    }
    if (message.lastUsageAt !== undefined) {
      obj.lastUsageAt = message.lastUsageAt.toISOString();
    }
    if (message.active !== false) {
      obj.active = message.active;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<OperatorAllowance>, I>>(base?: I): OperatorAllowance {
    return OperatorAllowance.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<OperatorAllowance>, I>>(object: I): OperatorAllowance {
    const message = createBaseOperatorAllowance();
    message.group = object.group ?? "";
    message.operator = object.operator ?? "";
    message.allowance = object.allowance ?? 0;
    message.usage = object.usage ?? 0;
    message.resetPeriodSeconds = object.resetPeriodSeconds ?? 0;
    message.lastResetAt = object.lastResetAt ?? undefined;
    message.lastUsageAt = object.lastUsageAt ?? undefined;
    message.active = object.active ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(long: Long): number {
  if (long.gt(globalThis.Number.MAX_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (long.lt(globalThis.Number.MIN_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return long.toNumber();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
