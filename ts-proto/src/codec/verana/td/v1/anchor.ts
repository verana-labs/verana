// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: verana/td/v1/anchor.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { Timestamp } from "../../../google/protobuf/timestamp";
import Long = require("long");

export const protobufPackage = "verana.td.v1";

/**
 * Anchor represents a group policy account as primary identity.
 * The anchor_id IS the group policy address.
 */
export interface Anchor {
  /**
   * anchor_id is the group policy account address.
   * This is the primary key and identifier.
   */
  anchorId: string;
  /** group_id is the x/group group ID that owns this policy. */
  groupId: number;
  /** created timestamp. */
  created:
    | Date
    | undefined;
  /** metadata for the anchor (optional description). */
  metadata: string;
}

/**
 * VerifiableService represents a hot operator key for an Anchor.
 * VS operators can act on behalf of the Anchor within their allowance.
 */
export interface VerifiableService {
  /** anchor_id this VS belongs to. */
  anchorId: string;
  /**
   * operator_account is the hot key address.
   * This is the primary key for VS lookup.
   */
  operatorAccount: string;
  /** registered timestamp. */
  registered:
    | Date
    | undefined;
  /** active status - can be deactivated without removal. */
  active: boolean;
  /** metadata (service description, endpoint, etc). */
  metadata: string;
}

/** OperatorAllowance defines spending limits for a VS operator. */
export interface OperatorAllowance {
  /** anchor_id (group policy address). */
  anchorId: string;
  /** operator_account is the VS hot key address. */
  operatorAccount: string;
  /** allowance_limit is the max spendable amount per period (in uvna). */
  allowanceLimit: number;
  /** spent tracks amount spent in current period (in uvna). */
  spent: number;
  /** reset_period in seconds (e.g., 86400 for daily). */
  resetPeriod: number;
  /** last_reset timestamp. */
  lastReset: Date | undefined;
}

function createBaseAnchor(): Anchor {
  return { anchorId: "", groupId: 0, created: undefined, metadata: "" };
}

export const Anchor = {
  encode(message: Anchor, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.anchorId !== "") {
      writer.uint32(10).string(message.anchorId);
    }
    if (message.groupId !== 0) {
      writer.uint32(16).uint64(message.groupId);
    }
    if (message.created !== undefined) {
      Timestamp.encode(toTimestamp(message.created), writer.uint32(26).fork()).ldelim();
    }
    if (message.metadata !== "") {
      writer.uint32(34).string(message.metadata);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Anchor {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAnchor();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.anchorId = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.groupId = longToNumber(reader.uint64() as Long);
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.created = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.metadata = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Anchor {
    return {
      anchorId: isSet(object.anchorId) ? globalThis.String(object.anchorId) : "",
      groupId: isSet(object.groupId) ? globalThis.Number(object.groupId) : 0,
      created: isSet(object.created) ? fromJsonTimestamp(object.created) : undefined,
      metadata: isSet(object.metadata) ? globalThis.String(object.metadata) : "",
    };
  },

  toJSON(message: Anchor): unknown {
    const obj: any = {};
    if (message.anchorId !== "") {
      obj.anchorId = message.anchorId;
    }
    if (message.groupId !== 0) {
      obj.groupId = Math.round(message.groupId);
    }
    if (message.created !== undefined) {
      obj.created = message.created.toISOString();
    }
    if (message.metadata !== "") {
      obj.metadata = message.metadata;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Anchor>, I>>(base?: I): Anchor {
    return Anchor.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Anchor>, I>>(object: I): Anchor {
    const message = createBaseAnchor();
    message.anchorId = object.anchorId ?? "";
    message.groupId = object.groupId ?? 0;
    message.created = object.created ?? undefined;
    message.metadata = object.metadata ?? "";
    return message;
  },
};

function createBaseVerifiableService(): VerifiableService {
  return { anchorId: "", operatorAccount: "", registered: undefined, active: false, metadata: "" };
}

export const VerifiableService = {
  encode(message: VerifiableService, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.anchorId !== "") {
      writer.uint32(10).string(message.anchorId);
    }
    if (message.operatorAccount !== "") {
      writer.uint32(18).string(message.operatorAccount);
    }
    if (message.registered !== undefined) {
      Timestamp.encode(toTimestamp(message.registered), writer.uint32(26).fork()).ldelim();
    }
    if (message.active !== false) {
      writer.uint32(32).bool(message.active);
    }
    if (message.metadata !== "") {
      writer.uint32(42).string(message.metadata);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): VerifiableService {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVerifiableService();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.anchorId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.operatorAccount = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.registered = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.active = reader.bool();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.metadata = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VerifiableService {
    return {
      anchorId: isSet(object.anchorId) ? globalThis.String(object.anchorId) : "",
      operatorAccount: isSet(object.operatorAccount) ? globalThis.String(object.operatorAccount) : "",
      registered: isSet(object.registered) ? fromJsonTimestamp(object.registered) : undefined,
      active: isSet(object.active) ? globalThis.Boolean(object.active) : false,
      metadata: isSet(object.metadata) ? globalThis.String(object.metadata) : "",
    };
  },

  toJSON(message: VerifiableService): unknown {
    const obj: any = {};
    if (message.anchorId !== "") {
      obj.anchorId = message.anchorId;
    }
    if (message.operatorAccount !== "") {
      obj.operatorAccount = message.operatorAccount;
    }
    if (message.registered !== undefined) {
      obj.registered = message.registered.toISOString();
    }
    if (message.active !== false) {
      obj.active = message.active;
    }
    if (message.metadata !== "") {
      obj.metadata = message.metadata;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VerifiableService>, I>>(base?: I): VerifiableService {
    return VerifiableService.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VerifiableService>, I>>(object: I): VerifiableService {
    const message = createBaseVerifiableService();
    message.anchorId = object.anchorId ?? "";
    message.operatorAccount = object.operatorAccount ?? "";
    message.registered = object.registered ?? undefined;
    message.active = object.active ?? false;
    message.metadata = object.metadata ?? "";
    return message;
  },
};

function createBaseOperatorAllowance(): OperatorAllowance {
  return { anchorId: "", operatorAccount: "", allowanceLimit: 0, spent: 0, resetPeriod: 0, lastReset: undefined };
}

export const OperatorAllowance = {
  encode(message: OperatorAllowance, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.anchorId !== "") {
      writer.uint32(10).string(message.anchorId);
    }
    if (message.operatorAccount !== "") {
      writer.uint32(18).string(message.operatorAccount);
    }
    if (message.allowanceLimit !== 0) {
      writer.uint32(24).uint64(message.allowanceLimit);
    }
    if (message.spent !== 0) {
      writer.uint32(32).uint64(message.spent);
    }
    if (message.resetPeriod !== 0) {
      writer.uint32(40).uint64(message.resetPeriod);
    }
    if (message.lastReset !== undefined) {
      Timestamp.encode(toTimestamp(message.lastReset), writer.uint32(50).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): OperatorAllowance {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOperatorAllowance();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.anchorId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.operatorAccount = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.allowanceLimit = longToNumber(reader.uint64() as Long);
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.spent = longToNumber(reader.uint64() as Long);
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.resetPeriod = longToNumber(reader.uint64() as Long);
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.lastReset = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OperatorAllowance {
    return {
      anchorId: isSet(object.anchorId) ? globalThis.String(object.anchorId) : "",
      operatorAccount: isSet(object.operatorAccount) ? globalThis.String(object.operatorAccount) : "",
      allowanceLimit: isSet(object.allowanceLimit) ? globalThis.Number(object.allowanceLimit) : 0,
      spent: isSet(object.spent) ? globalThis.Number(object.spent) : 0,
      resetPeriod: isSet(object.resetPeriod) ? globalThis.Number(object.resetPeriod) : 0,
      lastReset: isSet(object.lastReset) ? fromJsonTimestamp(object.lastReset) : undefined,
    };
  },

  toJSON(message: OperatorAllowance): unknown {
    const obj: any = {};
    if (message.anchorId !== "") {
      obj.anchorId = message.anchorId;
    }
    if (message.operatorAccount !== "") {
      obj.operatorAccount = message.operatorAccount;
    }
    if (message.allowanceLimit !== 0) {
      obj.allowanceLimit = Math.round(message.allowanceLimit);
    }
    if (message.spent !== 0) {
      obj.spent = Math.round(message.spent);
    }
    if (message.resetPeriod !== 0) {
      obj.resetPeriod = Math.round(message.resetPeriod);
    }
    if (message.lastReset !== undefined) {
      obj.lastReset = message.lastReset.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<OperatorAllowance>, I>>(base?: I): OperatorAllowance {
    return OperatorAllowance.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<OperatorAllowance>, I>>(object: I): OperatorAllowance {
    const message = createBaseOperatorAllowance();
    message.anchorId = object.anchorId ?? "";
    message.operatorAccount = object.operatorAccount ?? "";
    message.allowanceLimit = object.allowanceLimit ?? 0;
    message.spent = object.spent ?? 0;
    message.resetPeriod = object.resetPeriod ?? 0;
    message.lastReset = object.lastReset ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(long: Long): number {
  if (long.gt(globalThis.Number.MAX_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (long.lt(globalThis.Number.MIN_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return long.toNumber();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
